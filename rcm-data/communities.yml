# communities.yml
#
# NOT USED IN PRODUCTION. The list of communities lives in the Google Sheet,
# which is private. The scraper reads it through SHEET_API_URL and
# SHEET_TOKEN (GitHub > Settings > Secrets and variables > Actions).
#
# This file is only a fallback for running the scraper without a sheet, for
# example on a developer's machine. It is deliberately empty: this repository
# is public, and source URLs listed here would be visible to anyone.
#
# Format, if you ever need it locally (do not commit real URLs):
#
# - slug: example-community        # permanent code, never change it
#   nombre: Example Community
#   barrio: brickell
#   estrategia: auto               # auto | xhr | manual
#   identity: honest               # honest | impersonate
#   url: https://www.example.com/community-page
[]
